config:
    prompt-string: "$ "
    typing-color: bright-green
    typing-speed: supersonic

commands:
    - python3 --version
    - python3:
        - python3
        - import secrets
        - secrets.token_hex(16)
        - CTRL_D
    - whoami
