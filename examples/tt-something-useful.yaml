config:
    prompt-string: "$ "
    typing-color: cyan
    typing-speed: supersonic

commands:
    - echo ~
    - ls -l ~/.zshrc
    - cat ~/.zshrc
    - ssh:
        - ssh root@cardkist.com
        - hostname
        - exit
    - hostname
